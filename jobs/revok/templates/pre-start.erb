#!/bin/bash

set -e

if [ -f /var/vcap/jobs/revok/config/private_key ]; then
  chmod 0400 /var/vcap/jobs/revok/config/private_key
fi

if [ -f /var/vcap/jobs/revok/config/public_key ]; then
  chmod 0400 /var/vcap/jobs/revok/config/public_key
fi
