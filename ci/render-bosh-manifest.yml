---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: pivotalcf/cred-alert-ci

inputs:
- name: sec-red-meta
- name: cred-alert

outputs:
- name: manifest

params:
  MANIFEST_PATH:
  GITHUB_PRIVATE_KEY:
  GITHUB_PUBLIC_KEY:
  GITHUB_ACCESS_TOKEN:
  GITHUB_WEBHOOK_SECRET:
  MYSQL_USERNAME:
  MYSQL_PASSWORD:
  MYSQL_HOSTNAME:
  DATADOG_API_KEY:
  SENTRY_DSN:
  PAPERTRAIL_ADDRESS:
  SLACK_WEBHOOK_URL:
  IGNORE_REPOS:
  RPC_SERVER_CLIENT_CA:
  RPC_SERVER_CERT:
  RPC_SERVER_PRIVATE_KEY:
  RPC_SERVER_PRIVATE_KEY_PASSPHRASE:
  PUBSUB_PUBLIC_KEY:
  PUBSUB_PRIVATE_KEY:

run:
  path: cred-alert/ci/scripts/render-bosh-manifest
