---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang

params:
  ENVIRONMENT: must-set

inputs:
- name: cred-alert

outputs:
- name: cred-alert-app

run:
  path: cred-alert/ci/scripts/build-app
