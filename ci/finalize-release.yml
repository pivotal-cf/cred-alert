---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/bosh-cli

inputs:
- name: bosh-release
- name: bosh-rc
- name: version
- name: sec-red-deployments
  path: deployments

outputs:
- name: final-release-repo
- name: final-release-tarball

params:
  RELEASE_NAME:
  BLOBSTORE_PRIVATE_YML_PATH:

run:
  path: bosh-release/ci/scripts/finalize-release
