---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/bosh-cli

inputs:
- name: cred-alert
  path: bosh-release
- name: version
- name: bosh-rc
- name: sec-red-deployments
  path: deployments

outputs:
- name: final-release-repo
- name: final-release-tarball

params:
  RELEASE_NAME:

run:
  path: cred-alert/ci/scripts/finalize-release
