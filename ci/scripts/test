#!/usr/bin/env bash

set -e

export GOPATH=$PWD/gopath
export PATH=$GOPATH/bin:$PATH

service mysql start

pushd ${GOPATH}/github.com/pivotal-cf/cred-alert
  go install github.com/onsi/ginkgo/ginkgo

  ./scripts/test -race "$@"
popd
