#!/usr/bin/env bash

set -e -u

export GOPATH="${PWD}/cred-alert"
export PATH="${GOPATH}/bin:${PATH}"

OUTPUT="${PWD}/cred-alert-${OS}-cli"

go build -v -o "${OUTPUT}/cred-alert-cli" "cred-alert/cmd/cred-alert-cli"

