---
platform: darwin

inputs:
- name: credential-counter-binary

run:
  path: cred-alert/ci/scripts/deploy-credential-counter

params:
  LABEL:
  WORKDIR:
  PORT:
  RPC_SERVER_ADDRESS:
  RPC_SERVER_PORT:
  CA_CERT:
  CLIENT_CERT:
  CLIENT_KEY:
