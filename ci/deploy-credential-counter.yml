---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang

inputs:
- name: cred-alert
- name: credential-count-binary

run:
  path: cred-alert/ci/scripts/deploy-credential-counter

params:
  LABEL:
  WORKDIR:
  PORT:
  RPC_SERVER_ADDRESS:
  RPC_SERVER_PORT:
  CA_CERT:
  CLIENT_CERT:
  CLIENT_KEY:
